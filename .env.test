# Configuration pour les tests PHPUnit
# Base de données de test séparée

# Base de données TEST (localhost pour tests hors Docker)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=hangman_test_db
DB_USERNAME=hangman_user
DB_PASSWORD=hangman_password

# Environnement de test
APP_ENV=testing
APP_DEBUG=true
API_DEBUG=true

# Admin pour les tests (mêmes credentials que dev pour compatibilité)
FEATURE_ADMIN_PANEL=true
ADMIN_USERNAME=je_suis_admin
ADMIN_PASSWORD=liamambre1013

# Configuration API
API_CACHE_TTL=0
CACHE_ENABLED=false